image: innocentzero/rbenv
script:
  - sudo apt-get -y install libsqlite3-dev sqlite3 1> /dev/null 2> /dev/null
  - rbenv install 2.1.1
  - rbenv global 2.1.1
  - bundle
  - bundle exec rake
notify:
 email:
  recipients:
    - innocent.zero@gmail.com
